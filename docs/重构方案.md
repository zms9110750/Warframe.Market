# 重构方案（GUI 层，lib 不动）

基于旧版语义对比（WarframeMarketQueryWPF）+ 5 维审查 + 用户约束（lib 不动、预加载不做、MS 缓存/HTTP Fusion 双层不动、两套限流待议）。

## A. 语义修正（快、低风险）

| # | 文件 | 改动 | 语义 |
|---|---|---|---|
| A1 | `Pages/ArcaneTable.razor` | 日均交易量补 `/ SyntheticConsumption[maxRank]` 归一化 | 旧版语义：日均量按最大等级合成消耗归一化，当前偏大 |
| A2 | `Pages/ArcaneTable.razor` | 名称列恢复 ClickLink（加载时存 slug，AppState.ClickLink 时 ExternalLink.Open 链接物品页） | 旧版语义：赋能名可点跳物品页 |
| A3 | `Pages/SearchBox.razor` | 占位符文案对齐实现（"空格/· 为段分隔符" → "/ 分隔多词"） | 实现只按 `/`、`\` 分割 |

## B. Tab 生命周期收敛（消 ~150 行镜像）

| # | 文件 | 改动 |
|---|---|---|
| B1 | 新建 `Services/TabSessionState.cs` | `TabSessionState<TKey>`：Tabs/Pinned/ActiveIndex/PinMode + Cts 字典 + Add/Remove/Cancel/TogglePin/GetCts |
| B2 | 新建 `Pages/PinnedTabs.razor` | 合并 SearchTabs/UserTabs（参数化：tabs/pinned/pinMode + onSelect/onClose/onPin 回调） |
| B3 | `Pages/ItemSearch.razor` | 改用 TabSessionState + PinnedTabs（删 _tabs/_pinned/_tabCts/searchingTabs 等字段与重复方法） |
| B4 | `Pages/UserSearch/UserSearch.razor` | 同 B3 |

## C. 统计降级收敛

| # | 文件 | 改动 |
|---|---|---|
| C1 | 新建 `Services/StatPriority.cs` | `DemoteStatistics(IItemSearchService svc, IEnumerable<string> slugs, CacheItemPriority p)` 公共 helper；ItemSearch/UserSearch 复用（删各自 Demote*Statistics） |

## D. 记录（lib 不动，待 lib 轮处理）

- `OrderService.BuildColumns`/`OrderColumn`（lib 死接口，GUI OrderTop 内联为实际版本）
- `StatisticPrice.GetMaterialBasedReferencePrice`（无调用者）
- `UIConfig`/`StatusMessage`/`ItemSearchService._active`（死代码）
- 库 `WarframeMarketClient` 内置 Pipeline（GUI 不用，两套限流待议）

## 行为不变

- 106 测试保持全绿（无 ItemSearch/UserSearch 页面 bUnit，重构不破坏现有）
- 语义与当前一致（欢迎 tab/钉住/取消/降级行为不变，仅收敛实现）
- 构建 0 警告 0 错误（产物发布到本地临时目录，不入库）
