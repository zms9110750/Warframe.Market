# 重写项目差距 & TODO

---

## 已明确的架构决策

| 主题 | 决策 |
|------|------|
| **ORM** | EF Core 保留，去掉 FusionCache |
| **Trie 搜索** | 语义完全保留（段分隔符 `_` `空格` `·`、前导跳节、`/` 多词）。数据来源：只加载 SQLite 里已缓存的记录（slug + 所有 i18n Name） |
| **Tool 项目** | 是我的测试沙盒，实现先用它验证，最终不留 |
| **App 代码组织** | 一个文件夹放业务逻辑；所有 Razor 不得有 `@code`，写在 `.razor.cs` 里 |
| **快捷回复存储** | 手写 SQLite，EF Core 管理；可编辑时可读写，失焦时保存（空的不存），关闭编辑时只读 |
| **快捷查询（搜索历史）** | 原项目是 chip 纸片；改为 Masa 多标签页，点击开新标签；标签内没有输入框，输入框是独立的子组件 |
| **搜索交互** | 响应式：输入即搜，用 Rx 风格管道做节流（原项目 SplitField 语义）。不要 Enter/按钮触发 |
| **首页空白态** | 显示 README 内容 |
| **Masa 用法** | 不会的去读 GitHub 文档 |
| **组件状态** | 原项目组件隔离和状态管理很差，需重写。保留 `@key` 模式固定组件状态 |
| **订单查询** | 新 API 接受物品 ID 查询，不再需要 slug → 物品的映射步骤 |
| **渐进加载** | 原项目用 `ValueTask.IsCompleted` + 内存缓存当线程安全字典的 hack 要重写 |

---

## 一、WarframeMarketApp 差距

### 1.1 物品搜索 `/`

| # | 原项目语义 | 重写现状 | 状态 |
|---|-----------|---------|------|
| S1 | Trie 段分隔符搜索（`_` `空格` `·` 段，前导=跳过一节）`/` 多词 | 只有简单 Contains / 前缀匹配 | TODO |
| S2 | 列：中文名、英文名、参考价、满级价 | 有 | ✅ |
| S3 | 参考价三层：0级/满级/混合，按物品类型自动选 | 只有 0级 和 满级，缺混合 | TODO |
| S4 | 行展开 → OrderTop，含类型分支参数 | 无分支 | TODO |
| S5 | OrderTop 列：买/卖、联系(复制 /w)、卖家名、声誉、语言、价格、数量 + 类型特有列 | 缺声誉、语言列 | TODO |
| S6 | 搜索历史：原 chip → 现改用 Masa 多标签页 | 无 | TODO |
| S7 | 输入框单独子组件，标签内无输入框 | 无 | TODO |
| S8 | 渐进加载：先显示已有价格，后台任务完成再刷新 | hack 版 | TODO（重写） |

### 1.2 用户订单 `/user`

| # | 原项目语义 | 重写现状 | 状态 |
|---|-----------|---------|------|
| U1 | 输入用户名 → 公开订单 | 有 | ✅ |
| U2 | 列：中文名、英文名、订单价、参考价、差价、买卖 | 列：物品(slug)、类型、铂金、数量、等级、参考价、差价 | TODO |
| U3 | **新 API 直接用 ItemId 查**，不用额外 Slug 映射 | 当前还是 slug 逻辑 | TODO（利用新 API） |
| U4 | 差价 = 参考价 - 订单价（正=便宜） | 反了 | TODO |
| U5 | 404 → Alert "此用户不存在" | 通用 try/catch | TODO |
| U6 | 按买/卖分组 | 平铺 | TODO |
| U7 | 行展开 → OrderTop | 无 | TODO |
| U8 | 参考价带 filter（按订单的 Rank/Subtype/星星） | 无 | TODO |

### 1.3 赋能包 `/arcane`

| # | 原项目语义 | 重写现状 | 状态 |
|---|-----------|---------|------|
| A1 | YAML 读取配置 | 有 | ✅ |
| A2 | 期望值计算 + 购买量表头 [0,2,6,15,35] | ❌ | TODO |
| A3 | 流动性封顶 | ❌ | TODO |
| A4 | 行展开明细：出货率%、参考价、率×价、日均交易量 | ❌ | TODO |

### 1.4 快捷回复 `/reply`

| # | 语义 | 现状 | 状态 |
|---|------|------|------|
| R1 | 可编辑时输入框可读写 | 有 | ✅ |
| R2 | **失焦时保存（空的不存）** | 无保存 | TODO |
| R3 | **关闭编辑时只读** | 有 readonly | ✅ |
| R4 | **持久化到手写 SQLite（EF Core 管）** | TODO 未实现 | TODO |

### 1.5 布局/全局 — 版本/数据库按钮

版本/数据库按钮的完整语义（原项目 + 你的补充）：

| 条件 | 显示 | 点击行为 |
|------|------|---------|
| **无数据库 / 数据过期 / 强制刷新** | — | 先删表，然后拉取数据写入 |
| **有数据，版本正确** | `数据日期 yyyy-MM-dd`（仅年月日） | 点击 → 文字变为"再次点击强制刷新" |
| **显示"再次点击强制刷新"时再次点击** | — | 强制执行刷新（删表→拉取→写入） |
| **显示"再次点击强制刷新"时失去焦点** | — | 文字还原为 `数据日期 yyyy-MM-dd` |

当前重写版只有版本号查询和点击提示刷新，没有完整的删表→拉取→写入流程。**TODO**

其他全局开关与状态：

| # | 语义 | 现状 | 状态 |
|---|------|------|------|
| L1 | "打开链接"开关 | 硬编码 false | TODO |
| L2 | "可编辑"开关 | 有 | ✅ |
| L3 | Language / Platform / Crossplay | 有 | ✅ |
| L4 | 启动版本检查 + 自动更新索引 | 只查版本 | TODO |
| L5 | 搜索历史标签持久化到手写 SQLite | 无 | TODO |

### 1.6 组件/代码组织

| # | 要求 | 现状 | 状态 |
|---|------|------|------|
| C1 | Razor 无 `@code`，全在 `.razor.cs` | App 基本做到了，但 ItemSearch.razor 有 inline | TODO（拆分） |
| C2 | 业务逻辑放独立文件夹 | 当前 Services/ | ✅ 但需调整 |
| C3 | 组件状态用 `@key` 固定 | 部分有 | ✅ 保持 |
| C4 | 渐进加载重写：不再依赖 `ValueTask.IsCompleted` | 当前逐步加载方式需要重写 | TODO |
| C5 | 新 API 直接接受 ItemId，简化订单→物品查询 | Current code still uses slug pattern | TODO |

---

## 二、WarframeMarketTool（我的测试沙盒）

- 用于**我先在里面写代码验证**，不劳烦你开窗口
- 最终这个项目不留
- 所有实现问题我先在 Tool 里测通，再搬到 App

当前只是一个 Blind Rage 测试脚本。

---

## 三、参考价语义

### 算法

```
取 StatisticsClosed.Day90 中最近 7 天
  ↓
按 subtype/quality 过滤（0级/满级/混合各自不同 filter）
  ↓
每天取 Median
  ↓
7 天的 Median 做加权平均
  权重 = 时间权重 [40,25,15,5,5,5,5] × 当天 Volume
  ↓
结果 = Σ(权重 × Median) / Σ(权重)
```

**要点**：
- 只用 **Closed**（已结算）数据，不用 Live
- 买卖单都算
- 时间权重固定 `[40,25,15,5,5,5,5]`，第1天（最近）权重40，第7天权重5
- 最终权重 = 时间权重 × 当天交易量
- 如果某天没有符合 filter 的数据则跳过

### 三种价格

| 价格 | filter |
|------|--------|
| **0级价** | `ModRank is null or 0`, `AmberStars <= 0`, `Subtype` 排除 Crafted/Radiant |
| **满级价** | `ModRank > 0`, Subtype 为 Crafted/Radiant/Magnificent/Large |
| **混合价** | 满级价 ÷ `SyntheticConsumption[rank]`（仅赋能用） |

### 物品类型 → 显示 + OrderTop 分支

| 类型 | 显示价 | OrderTop 分支 |
|------|-------|-------------|
| 普通物品 | 0级 | 无 |
| MOD | 0级 + 满级 | 不满级 + 满级 |
| 赋能 | 0级 + 满级 | 不满级 + 满级 |
| 遗物 | 0级 + 满级 | 完整 + 光辉 |
| 裂罅 | 0级 | 已揭示 + 未揭示 |
| 可制作组件 | 0级 + 满级（混合） | 蓝图 + 成品 |
| 阿耶檀识 | 0级 + 满级 | 不满星 + 满星 |
| Prime 部件 | 0级 | 无 |
| 装备 | 0级（混合） | 无 |

---

## 四、当前重写 App 要保留的代码

- **Wpf Blazor Hybrid 壳**（MainWindow.xaml + BlazorWebView）— 保留
- **Masa.Blazor 组件库** — 保留
- **EF Core + SQLite 数据库** — 保留（去掉 FusionCache 即可）
- **WarframeMarketClient**（来自 Api 库）— 保留，利用其新特性（ItemId 查询等）
- **NavItemAttribute** 导航注册 — 保留
- **路由结构** — 保留

## 五、当前重写 App 要去掉的代码

- **FusionCache / HybridCache** 相关的任何引用
- **LocalCacheService**（它的逻辑合并进手写 SQLite 层，或去掉）
- **ItemsCacheService**（缓存策略改为 EF Core 直接管理）
- **SearchService**（搜索逻辑统一进 ItemsService 或其他业务层）
- 所有 TODO 标记的未实现片段在重写时会自然被替换

## 六、需要去读的

- Masa.Blazor 的 **Tabs / PTTabs** 组件文档（用于快捷查询多标签）
- Masa.Blazor 的 **PCopyableText** 组件（原项目用了复制文本）
- `Microsoft.Data.Sqlite` 裸 ADO.NET（如果需要在 EF Core 之外跑原始 SQL）
