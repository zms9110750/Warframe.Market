# Warframe.Market 原项目语义分析（AI 工作用 README）

> 本文档分析 **WarframeMarketQuery** 和 **WarframeMarketQueryWPF** 的前端语义、用户意图、领域概念。
> 不涉及 FusionCache/Refit/Polly 等实现细节，只关注"用户能做什么"和"为什么这样做"。

---

## 一、整体定位

这是一个 **Warframe 玩家辅助交易工具**，以 Warframe.Market 网站公开数据为基础，提供：

1. **快速比价** — 查某件物品当前的市场参考价和在线卖单
2. **对手分析** — 看某个卖家/买家所有的公开订单，找出差价机会
3. **收益计算** — 评估"开赋能包"的期望收益，辅助决策
4. **交易辅助** — 一键复制交易喊话模板、快捷回复

核心价值：**不需要开浏览器，在桌面端完成交易决策的全流程**。

---

## 二、领域概念模型

### 2.1 核心领域对象

| 概念 | 语义 | 用户理解 |
|------|------|---------|
| **Item（物品）** | Warframe 中可交易的一切 | 战甲、武器、MOD、赋能、遗物、鱼、塑像等 |
| **Order（订单）** | 玩家发布的买卖意愿 | Buy = 我想买 / Sell = 我想卖，含价格、数量、等级 |
| **OrderTop（Top订单）** | 当前在线玩家的前5个最优报价 | "现在谁能交易、多少钱" |
| **User（玩家）** | Warframe.Market 上的用户 | 有信誉分、在线状态、平台、语言 |
| **Statistic（统计数据）** | 历史成交的统计聚合 | 90天内的中位数、成交量、最高最低价 |
| **ArcanePack（赋能包）** | 游戏中获取赋能的方式（箱子） | 开这个箱子平均能赚多少白金 |
| **ReferencePrice（参考价）** | 由历史成交加权计算出的合理价格 | "这玩意儿大概值多少" |
| **Trie（搜索索引）** | 物品的多语言名称搜索树 | 输入中文/英文/ID 都能找到东西 |

### 2.2 物品的类型体系

物品在交易中有不同的"品种"概念，直接影响如何查价：

| 品种 | 价格细分方式 | 示例 |
|------|------------|------|
| **普通** | 只有一个价格 | Wisp Prime 蓝图 |
| **可升级（MOD/赋能）** | 分等级（Rank 0–10），等级越高越贵 | 盲怒、充沛 |
| **可制作** | 分蓝图 vs 成品 | 葬棺之锤 蓝图/成品 |
| **遗物** | 分完整/优良/无暇/光辉 | 锂稀薄1 光辉 |
| **裂罅** | 分已揭示/未揭示 | 已揭示的布拉玛裂罅 |
| **阿耶檀识** | 分琥珀星/青蓝星数量 | 安柏塑像（满星/空星） |

---

## 三、用户能做的一切（按场景组织）

### 场景 A：我想卖/买某件东西 → 查当前市场价格

**用户流程**：
1. 在主搜索框输入物品名（中文/英文/ID 都行）
2. 看搜索结果表：**参考价** 和 **满级价**（如适用）
3. 展开某行 → 看当前在线的 **买家前5报价** 和 **卖家前5报价**
4. 如果差价比参考价低很多 → 有套利空间
5. 点击"联系"按钮 → 复制 `/w` 喊话模板，在游戏中粘贴发送

**语义要点**：
- 搜索是**前缀匹配 + 多段忽略**（`w 一套` 能找到 "Wisp Prime 一套"，空格是段分隔符）
- `/` 或 `\` 分隔多个搜索词，可以一次查多个物品
- "参考价"和"满级价"是两条不同维度的价格线
- 喊话模板是 `/w 用户名 Hi! I want to buy: "英文名"中文名，for XX platinum.`

**数据源头**：物品索引（只下载一次）→ 统计数据（90天聚合）→ 当前在线订单（实时）

---

### 场景 B：我想跟某个人交易 → 看他所有公开订单

**用户流程**：
1. 输入 Warframe.Market 用户名
2. 看到该用户**所有公开订单**，按买卖分组
3. 每行显示：物品名称、**订单价 vs 参考价**、差价
4. 差价绿色=他的价比参考价低（你买划算）、红色=比参考价高（你卖给他划算）
5. 展开某行 → 看这件物品的当前全市场在线订单（防止他挂低价但没货）

**语义要点**：
- 这是**对手分析**工具：查清一个人所有挂单，看哪些值得交易
- 差价列是核心决策指标：`参考价 - 订单价`
- 订单按买卖分组 → 一眼区分他是买家还是卖家

**数据源头**：用户订单接口 → 映射到物品信息 → 统计数据

---

### 场景 C：我想靠开赋能包赚钱 → 算期望收益

**用户流程**：
1. 进入"赋能包查询"页面
2. 看表格：每种赋能包在**不同购买量**下的期望价值
3. 判断：如果交易所里有人在 80p 卖一组"小小黑赋能"，而开某个包的期望值是 120p → 有利可图
4. 如果某个包在大量购买（35组/天）时期望值暴跌 → 说明这包里的赋能卖不动，会滞销

**语义要点**：
- 这是**投产比计算器**：投入（买分解材料的钱）vs 产出（开出赋能的期望价值）
- 多个日购买量维度（0、2、6、15、35）→ 模拟从自用到批量倒卖的场景
- 行展开后显示明细：**出货率 × 价格** → 让你看到哪些赋能是价值支柱
- 日均交易量列 → 判断流动性，防止砸手里

**计算公式的领域含义**：
```
期望价值 = Σ( 每个赋能的开出概率 × 它的参考价 ) × 每组能开的赋能数
           ↓                          ↓                   ↓
          这把能开出什么             值多少钱            一组材料能开多少次
```

当购买量很大时，市场每天只能消化那么多 → 按成交量上限而不是概率算。

**数据源头**：YAML 配置（预定义的包构成）→ 统计数据 → 参考价

---

### 场景 D：我要在游戏中快速回复 → 管理一段常用文本

**用户流程**：
1. 进"快捷回复"页面
2. 开启"可编辑" → 添加/删除常用的回复文本
3. 关闭编辑 → 看到每条文本旁边的复制按钮
4. 点击复制 → 粘贴到游戏聊天框

**语义要点**：
- 解决"游戏中打字麻烦"的痛点
- 可自定义，每个人需要的喊话/报价文本不一样
- 数据只存在本地，不需要同步

**数据源头**：纯本地缓存

---

### 场景 E：我需要保持数据最新 → 更新/下载

**用户流程**：
1. 进"更新客户端"页面
2. 看 Gitee 上的发布版本列表
3. 点击下载 → 自动下载并解压替换

**语义要点**：
- 桌面端应用需要**自动更新**机制
- 使用 Gitee 作为发布渠道（面向中国用户）
- 支持 .tgz 一键解压（减少手动操作步骤）

**数据源头**：Gitee API

---

## 四、全局开关的语义

| 开关 | 语义 | 影响 |
|------|------|------|
| **可编辑** | 用户是否处于"整理数据"模式 | 关：只能查看和复制；开：能增减删改 |
| **打开链接** | 数据展示是"信息"还是"入口" | 关：文本可直接复制；开：文本变为浏览器链接 |

这两个开关揭示了一个设计哲学：**同一个界面在不同意图下有不同的交互方式**。

---

## 五、原项目表达的设计原则

### 5.1 离线优先
- 物品索引全量下载到本地 → 搜索不依赖网络
- 统计数据有较长缓存 → 断网仍可看参考价
- 用户感觉"秒开"，即使是第一次搜索

### 5.2 渐进式加载
- 页面先显示已有数据、再刷新计算结果
- 不设全局 loading，哪个数据就绪就显示哪个
- 用户感知：流畅、不卡死

### 5.3 复制优先
- 几乎所有文本值都可点击复制
- `/w` 交易喊话模板一键生成并复制
- 典型场景：在游戏中 Alt+Tab 到工具 → 搜索 → 复制 → 回游戏粘贴

### 5.4 不带认证的只读工具
- 所有数据来自 Warframe.Market 公开 API（免登录）
- 不涉及下单、修改订单等操作
- 用户不需要登录即可使用全部功能

### 5.5 面向中文玩家的英语游戏工具
- 界面英文（匹配游戏语言），注释和说明中文
- 所有内部提示/配置/文档使用中文
- 默认语言设置为 zh-hans

---

## 六、重写时需要保留的语义契约

以下是不管用什么技术栈，都必须满足的能力：

| 功能 | 必须保证的语义 |
|------|--------------|
| 物品搜索 | 前缀匹配 + 段分隔符跳过 + 多语言 + 多词同时查 |
| 参考价 | 90天加权中位数，可过滤等级/子类型 |
| 在线订单 | 前5买/前5卖，按物品类型细分查询参数 |
| 用户订单 | 映射物品名 + 算差价 |
| 赋能包 | YAML 定义包结构 + 概率 x 价格 + 流动性封顶 |
| 快捷回复 | 纯本地存一段文字 |
| 更新 | 下载并替换自身可执行文件 |

---

## 七、关键数值的领域含义

| 数值 | 值 | 含义 |
|------|----|------|
| 参考价权重 | `[40,25,15,5,5,5,5]` | 最近7天的数据各有权重（最近的最重要），然后第2周25，第3周15，后面四周各5。总计90天。 |
| 合成消耗 | `[1,3,6,10,15,21]` | 把一个赋能从等级0合到等级N需要多少个一级赋能。满级价 ÷ 消耗 = 一级等价价。 |
| PackGainRate | `420×6÷200×3` | 玩家俗称"一组小小黑"能开多少次赋能包。420=一次获取的荧尘，6=一组小小黑含6个，200=一个包的价格，3=一个包开出3个赋能。所以一组小小黑材料能开出来的赋能数。这个数值是37.8。 |
| 日购买量表头 | `[0, 2, 6, 15, 35]` | 0=自己开来自用不卖；2=少量倒卖；6/15=中量；35=大量批发。数字越大，流动性封顶效应越明显。 |
