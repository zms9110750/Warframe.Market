# TODO

### 1. 物品搜索子面板默认值
- 购/售开关默认在"售"（`_showBuy = false`）
- 满级开关默认关闭（`_showMaxRank = false`）

### 2. 用户订单子面板（OrderTop）的开关依订单状态
- 如果订单是卖单，默认显示售；买单默认显示购
- 如果订单有等级，满级开关默认开；否则默认关

### 3. 参考价、差价显示一位小数
- 当前 `ToString("F0")` 不显示小数 → 改为 `ToString("F1")`

### 4. 物品名称、英文名称加复制按钮
- 名称文本前面加一个复制图标按钮
- 复制时裁剪掉末尾的 " 一套" 和 " Set"
- 查看原项目（WarframeMarketQueryWPF）中此处的实现
