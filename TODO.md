# TODO

## P0

### 1. 用户面板物品名显示原始 ID 而非 i18n

`UserSearch.GetItemName` 返回 `o.ItemId` 截断字符串。需要从 `Item.I18n` 取中文名。

### 2. 用户面板子面板弹不出

`GetItemShort(ctx.Item)` 返回 null → `OrderTop.TargetItem` 为 null → 渲染"没有必需参数"。需要确保展开时数据已加载。

### 3. 强制刷新按钮没有加载状态

点击版本按钮 → "再次点击强制刷新" → 再次点击 → `RefreshAllAsync()` 执行期间按钮没有 loading 状态，可重复点击。

### 4. 快捷回复/钉住标签持久化到 YAML

当前用 EF Core SQLite 存 `QuickReplyItem`。改为 YAML 文件存储：快捷回复文本 + 搜索查询钉住列表 + 用户查询钉住列表。

### 5. 搜索：每个 `/` 段一张独立表格

当前 "盲怒/wisp/镀层" 搜索全部结果合并到一张表。要求：每个词一张独立表格，每张可折叠，只显示自己的结果。

### 6. 可编辑模式：标签显示图钉图标

开启可编辑后，标签上出现图钉图标。点击后持久化该标签（存到 YAML），始终显示。

### 7. 可编辑模式：钉住的标签显示解绑图标

已钉住的标签显示解绑图标，点击从持久化中删除。

### 8. 只读模式：钉住标签不显示关闭

关闭可编辑后，钉住的标签不显示关闭（×）按钮，普通标签仍可关闭。
