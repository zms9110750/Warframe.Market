# TODO

## P0

### 1. 标签放在搜索框上面

当前：搜索框 → 标签。要求：标签 → 搜索框。钉住的标签始终在顶部。

### 2. 初始标签总是带搜索框的页面

钉住的标签在初始化时自动加载，占据标签栏。搜索框作为独立控件在标签下方。

### 3. 可编辑模式：图钉/解绑互斥

当前：钉住时同时显示 pin 图标和解绑按钮。要求：钉住的只显示解绑图标，未钉住的只显示图钉图标，不共存。

### 4. 钉住标签页不显示搜索框

当前：所有标签页都显示 SearchBox。要求：钉住的标签页（从持久化加载的）不显示搜索框，只显示搜索结果。

### 5. 搜索表没有折叠

`SearchResultTable` 需要可折叠（每个搜索词的结果表可以收起/展开）。

### 6. 用户订单显示物品 ID 而非 i18n

`UserSearch.GetItemName` 在物品未加载时返回原始 ItemId。要求：始终显示中文名或英文名。

### 7. 用户订单没有参考价和差价

当前：参考价和差价列没有正确渲染。要求：
- 显示参考价：满级订单显示满级价，否则显示 0 级价
- 显示差价：参考价 - 订单价

### 8. 用户订单显示英文名和语言

要求：用户订单表增加"英文名称"列，以及用户的语言信息。

### 9. 快捷回复保存后输入栏不清空

`AddNew` 后输入框仍然显示旧文本。
