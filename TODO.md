# TODO

## P0

### 1. 物品搜索：常驻"搜索"标签

当前：没有钉住标签时只显示搜索框，钉住后只有钉住的标签。
要求：
- 第一个标签永远是"搜索"标签，显示搜索框，不可关闭、不可钉住/解绑
- 钉住的标签从第二个开始，自动加载搜索结果
- 搜索标签始终存在，即使用户没有钉住任何搜索

### 2. 物品搜索子标签展开报错

展开搜索结果行（OrderTop）时报错，可能是 `ExpandedItemContent` 的 `context.Item2` 类型不匹配。

### 3. 用户订单支持 `/` 分隔多用户

类似物品搜索：输入 "realacestar/zms9110750" 按 `/` 分隔为多个用户名，每个用户一张独立表格，可折叠。

### 4. 用户订单常驻搜索标签 + 固定标签

- 第一个标签永远是"搜索"标签（输入用户名）
- 支持钉住用户查询（图钉图标）
- 钉住的用户查询自动加载
- 可编辑模式下显示图钉/解绑
