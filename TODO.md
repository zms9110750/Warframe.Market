# TODO - 未修复问题清单

对照用户反馈，逐条记录当前行为与要求行为的差距。

---

## 1. 搜索：查询按钮不触发刷新

**用户报告**：点了"查询"按钮以后没有刷新，点清空才刷出来。

**原因分析**：`SearchBox` 用 `@bind-Value:event="oninput"`，但按钮（`AppendIconClick`）触发 `SubmitSearch()`，此时 `query` 的值可能不是用户最新输入的值。Blazor 的双向绑定和 `oninput` 事件可能存在竞态。

**要求**：
- 点击搜索图标或按回车，立即用输入框当前文本搜索
- 结果表格立即出现

**当前状态**：❌ 未修复

---

## 2. 赋能包：出货率不应与价格一起异步加载

**用户报告**：展开子面板后，出货率% 应该立刻显示（不是"加载中"），因为它来自 YAML 配置，不需要 API。

**要求**：
- 出货率% 从 YAML 直接计算，即时显示
- 参考价、出货率×价、日均交易量 异步加载（从 API）
- 出货率% 列不依赖任何 API 请求

**当前状态**：✅ 理论已修复（ArcaneTable 的 `Pack.GetProbability` 直接从配置读），需要验证。

---

## 3. 加载极慢

**用户报告**：加载不像一秒 3 次请求的速度，应该更快。

**原因分析**：`ArcaneTable.LoadItemDataAsync` 对每个物品依次执行 `SearchAsync` + `GetStatisticsAsync`，每次 `GetStatisticsAsync` 都是一次 HTTP 请求。90 天的统计数据从 API 获取。

**要求**：
- API 请求要有缓存（统计结果缓存到内存）
- 对于同一次会话，同一个物品的统计不应重复请求
- 加载速度应该接近 API 的速率限制上限（3 req/s）

**当前状态**：⚠️ 加了 IMemoryCache（10 分钟过期），加了每 3 个请求等 500ms。但第一次加载仍然需要逐个请求，速度受限于 API。

---

## 4. 所有请求都要有缓存

**用户报告**：开关一次子面板，价格就没有了（重新加载）。

**要求**：
- 统计数据的缓存应该跨页面切换/面板开关
- 不应该在面板关闭时清除缓存
- 再次打开同一面板应瞬间显示已有数据

**当前状态**：❌ 未验证。ArcaneTable 用了 `@key="Pack.Name"`，切换面板会销毁重建组件，但 `IMemoryCache` 是单例，数据应该还在。

---

## 5. 子面板加载完不刷新

**用户报告**：子面板所有数据都加载出来的时候，面板没刷新。没有刷新机制。

**原因分析**：原项目使用 `while(!allTasks.IsCompleted){ StateHasChanged(); await Task.Delay(200); }` 模式渐进刷新。当前 ArcaneTable 也有这个循环，但可能没有正确触发。

**要求**：
- 数据加载过程中，面板应逐渐显示已完成的条目（而不是全部加载完才一次性显示）
- 所有数据加载完成后，面板应刷新一次确保显示完整

**当前状态**：⚠️ 加了 `do { StateHasChanged(); await Task.Delay(200); } while (!allTasks.IsCompleted)`。需要验证是否生效。

---

## 6. 整体架构要求

来自用户的整体批评：

- **"让你重写，是让你写能实现的那种。你没有更好的实现，该抄要抄。"**
  - 原项目的实现模式（渐进加载、Task.WhenAll、IsCompleted 检查）被验证是可行的
  - 不应为了"不同而不同"去重新发明轮子
  - 如果某个模式在原项目里工作良好，直接采用

- **关于内存缓存**：
  - 不要手写 `ConcurrentDictionary` / `Dictionary` 当缓存
  - 用 `IMemoryCache`（Microsoft.Extensions.Caching.Memory）
  - 已切换为 IMemoryCache（需验证是否正常工作）

---

## 优先级排序

| 优先级 | 问题 | 影响 |
|--------|------|------|
| P0 | 搜索按钮不刷新 | 核心功能不可用 |
| P0 | 赋能包子面板不刷新 | 看不全数据 |
| P1 | 加载速度慢 | 用户体验差 |
| P1 | 开关面板数据丢失 | 重复请求浪费 |
| P2 | 出货率异步加载 | 显示延迟 |
