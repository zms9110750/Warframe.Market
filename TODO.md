# TODO — 未验证问题

以下是你提过的、我改了的、但你还**没跑过/没确认过**的问题：

| # | 问题 | 我改了什么 | 需要你确认 |
|---|------|-----------|-----------|
| 1 | 排序 key 重复导致排一列全排 | 每列用唯一 key（`"name_cn"`、`"name_en"`、`"price"`、`"price_max"`） | 跑一下排序，看还不会不会一列排序全排 |
| 2 | 打开链接英文名没变链接 | SearchResultTable 英文名加了 `<a>` 链接 | 打开顶栏"打开链接"开关，点英文名看是否跳转 |
| 3 | 售/购开关出现"全部" | 改回 `MSwitch`，只有购/售，没有第三个选项 | 拨一下看能不能正常切换购/售 |
| 4 | 0级/满级开关显示"全部等级" | 改回 `MSwitch`，显示"0级"/"满级"，切时重新请求 API | 拨一下看数据是否正确切换，不混在一起 |
| 5 | 搜索词显示两次 | 删了 `SearchTermGroup` 里重复的 `<p>` | 搜一个词看标题是不是只出现一次 |
| 6 | 子表出现下拉框 | 改回 `MSwitch` | 同3 |
| 7 | 等级列显示子类型 | 没改，需要你告诉我具体哪里错了 | 截个图或描述一下 |
